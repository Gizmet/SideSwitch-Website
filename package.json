{
  "name": "chat-roulette-dashboard",
  "version": "1.0.0",
  "description": "A dashboard for managing multiple chat roulette sites",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "lint": "eslint src/",
    "clean": "rm -rf dist/",
    "setup": "npm install && npm audit fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^28.1.0",
    "electron-store": "^8.1.0"
  }
}